# Database
DATABASE_URL="postgresql://keyhub:keyhub@localhost:6789/keyhub"

# NextAuth
NEXTAUTH_SECRET=""          # openssl rand -base64 32
NEXTAUTH_URL="http://localhost:4200"  # Remove this on Vercel (auto-detected)
AUTH_TRUST_HOST=true        # Required for Vercel/proxy deployments

# AES-256 key for encrypting provider keys (64 hex chars = 32 bytes)
KEY_ENCRYPTION_SECRET=""    # openssl rand -hex 32

# Resend (email)
RESEND_API_KEY=""
EMAIL_FROM="KeyHub <onboarding@resend.dev>"
